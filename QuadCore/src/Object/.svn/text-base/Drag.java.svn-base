package Object;

import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;

public class Drag extends Ball
{
	
	public Drag(int radius, int x, int y)
	{
		setX(x);
		setY(y);
		setRadius(radius);	
	}
}